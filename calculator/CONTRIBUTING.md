# Contributing

1. Keep the calculator dependency-free (standard library only).
2. Add `pytest` coverage for every new feature.
3. Run the suite from the `calculator/` directory before opening a pull request:

   ```bash
   pytest
   ```

Commit messages follow [Conventional Commits](https://www.conventionalcommits.org/).
