# Contributing

1. Keep the task manager dependency-free (standard library only).
2. Add `pytest` coverage for every new feature.
3. Run the suite from the `todo/` directory before opening a pull request:

   ```bash
   pytest
   ```

Commit messages follow [Conventional Commits](https://www.conventionalcommits.org/).
